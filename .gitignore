# ── OS / editor ──
.DS_Store
._*
~$*

# ── Dev ──
node_modules/
.venv/
venv/
__pycache__/
*.pyc
dist/
build/
.env

# ── Raw OFCI source dumps — kept out for size/rawness (181MB, contract PDFs, PII), not tracked ──
ofci data/
ofci data whole/
ofci case/
archive/
